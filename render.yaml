services:
  - type: web
    name: fiyat-karsilastirma
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 4 --timeout 120 --worker-class gthread --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
